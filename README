This is an example app for using Uploadify (http://www.uploadify.com) with Stripes.

To try this out.
1. Create the directory /tmp/uploads (or modify the path in UploadifyActionBean)
2. run mvn jetty:run
3. Open your browser to http://localhost:8080
